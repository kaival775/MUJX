{
  "name": "let-go-3.0",
  "version": "1.0.0",
  "description": "Root configuration for Let Go 3.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uvicorn main:app --reload\"",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && uvicorn main:app --reload\"",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}