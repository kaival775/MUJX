{
  "name": "let-go-3.0",
  "version": "1.0.0",
  "description": "Root configuration for Let Go 3.0",
  "scripts": {
    "start": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python main.py\"",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && python main.py\"",
    "build": "npm install && npm install --prefix frontend && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix frontend",
    "frontend": "npm run dev --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": "24.x"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "vite-plugin-pwa": "^0.19.8"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
